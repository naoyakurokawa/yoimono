<h1>ユーザー</h1>
<%= image_tag @user.image.url, size: '300x230', class:'' %>
<section class="stats">
    <%= render 'shared/stats' %>
</section>
<%= render 'follow_form' if logged_in? %>
<p><%= @user.name %></p>